#!/usr/bin/env bash

NUM=$1
if [ -z "${NUM}" ]; then
    echo "Usage: $0 <pr-number>"
    exit 1
fi
REMOTE=${REMOTE:-upstream}

git fetch "${REMOTE}" pull/${NUM}/head:${REMOTE}/pr/${NUM}
git checkout ${REMOTE}/pr/${NUM}
